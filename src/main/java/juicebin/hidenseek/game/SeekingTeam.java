package juicebin.hidenseek.game;

import net.kyori.adventure.text.format.TextColor;

public class SeekingTeam extends AbstractTeam {
    public SeekingTeam(String id, String displayName, TextColor color, String prefix, String suffix) {
        super(id, displayName, color, prefix, suffix);
    }
}
